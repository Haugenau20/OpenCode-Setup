# Placeholder so squid's `include /etc/squid/extra-allowlist.d/*.conf` glob
# always matches at least one file — an empty match is a fatal startup error.
#
# Drop your own per-developer *.conf files alongside this one to extend the
# allowlist (they are gitignored). Each file uses the same form as
# allowlist.d/, e.g.:
#
#   acl allowed_dst dstdomain .internal.example.com
#
# This file intentionally contains no directives.
